# PROJECT_CONTEXT 更新履歴: auto-net preset

## 変更理由
- consumer-facing template に、safe default を維持したまま network access つき自律実装を行う `auto-net` preset を追加した。

## 更新内容
- `repo_auto_net` profile と `.codex/rules-auto-net/` を現在の設計原則に追加。
- wrapper default は `safe`、auto-net は明示指定必須であることを記録。
- auto-net でも削除・git staging/commit/push 系は禁止であることを補足。
